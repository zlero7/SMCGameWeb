# Docker Compose 환경 변수 예시 파일
# 이 파일을 복사하여 .env로 저장하고 실제 값으로 수정하세요
# cp .env.example .env

# MySQL 비밀번호 (docker-compose.yml에서 사용)
MYSQL_ROOT_PASSWORD=your-root-password-here
MYSQL_PASSWORD=your-db-password-here
